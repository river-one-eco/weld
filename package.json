{
  "name": "weld",
  "version": "1.0.0-rc.2",
  "description": "Solidity formatter and linter for the Sky ecosystem",
  "author": "River Credit Labs",
  "license": "AGPL-3.0-or-later",
  "bin": {
    "weld": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node test/version.mjs && node test/run.mjs",
    "test:roundtrip": "node test/run.mjs --roundtrip"
  },
  "comment:dependencies": "Versions are pinned EXACTLY (no ^ or ~) for supply-chain safety; upgrades are deliberate. Install with `npm ci` to honour package-lock.json integrity hashes.",
  "dependencies": {
    "@solidity-parser/parser": "0.18.0",
    "chalk": "5.6.2",
    "commander": "12.1.0",
    "fast-glob": "3.3.3"
  },
  "devDependencies": {
    "@types/node": "22.19.17",
    "tsx": "4.21.0",
    "typescript": "5.9.3"
  }
}
